<?php
/*$submissionIdToDelete = 1; // Replace with the actual submission ID to delete

try {
    $stmt = $conn->prepare("DELETE FROM UserSubmissions WHERE submission_id = :id");

    // Bind parameters
    $stmt->bindParam(':id', $submissionIdToDelete);

    // Execute the statement
    $stmt->execute();

    echo "Submission deleted successfully!";
} catch (PDOException $e) {
    echo "Error: " . $e->getMessage();
}
?>*/
